import { NextPage } from "next";
import PrayerRequestForm from "../../components/prayer-request";

const PrayerRequest: NextPage = () => {
  return <PrayerRequestForm />;
};

export default PrayerRequest;
